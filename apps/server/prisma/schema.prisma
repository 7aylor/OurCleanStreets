generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("ocs-db")
}

model books {
  id     Int    @id(map: "test_pk")
  title  String @db.VarChar
  author String @db.VarChar
}
